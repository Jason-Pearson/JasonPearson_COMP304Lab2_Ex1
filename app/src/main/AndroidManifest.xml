<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jason.jasonpearson_comp304lab2_ex1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DisplayCuisineActivity" />
        <activity android:name=".DisplayAmericanActivity" />
        <activity android:name=".DisplayChineseActivity" />
        <activity android:name=".DisplayMexicanActivity" />
        <activity android:name=".DisplayIndianActivity" />
        <activity android:name=".DisplayKoreanActivity"></activity>
    </application>

</manifest>